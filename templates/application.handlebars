<h3>Github Issues for {{repositoryName}}</h3>
{{#if user}}
  <h3>My Issues:</h3>
  {{#if myIssues}}
    {{render 'issues' myIssues}}
  {{else}}
    <ul class="list"><li>No open issues assigned to you.</li></ul>
  {{/if}}
  <h3>All Issues:</h3>
{{/if}}
{{render 'issues' this}}
